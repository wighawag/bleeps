<script lang="ts">
  import {wallet, fallback} from '$lib/stores/wallet';
</script>

<!-- TODO pregenerate all sounds as wav so it can always be viewable without a node-->

<!-- <div style="display: grid;grid-template-columns: auto auto auto auto; gap: 1em; padding: 1em;"> -->
<div class="grid grid-cols-4 xl:grid-cols-8 gap-4 p-4">
  <!-- {#each ids as bleepId}
    <BleepsSvg id={bleepId} minted={true} disabled={false} pending={false} your={false} />
  {/each} -->
  <!-- {#if loading}
    Please wait...
  {:else}
    <img src={metadata.image} alt={metadata.name} />
    <audio src={metadata.animation_url} preload="auto" controls autoplay crossorigin="anonymous" class="mx-auto m-2" />
  {/if} -->

  TODO
</div>
