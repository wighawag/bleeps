{
	"private": true,
	"name": "booking-service",
	"version": "0.0.1",
	"type": "module",
	"description": "Booking service to let users advertise their purchase intent to each other, so two people do not spend gas on the same Bleep",
	"scripts": {
		"generate": "ldenv -d localhost node generate.cjs ../contracts/deployments/@@MODE @@",
		"dev": "ldenv -d localhost wait-on ../contracts/deployments/@@MODE@:@:/BleepsInitialSale.json ~~ pnpm generate @@MODE ~~ wrangler dev --port 8787 ~~ @@",
		"deploy": "ldenv pnpm generate @@MODE ~~ wrangler deploy ~~ @@",
		"typescript": "tsc",
		"format": "prettier --write .",
		"format:check": "prettier --check ."
	},
	"author": "wighawag <wighawag@gmail.com>",
	"devDependencies": {
		"@cloudflare/workers-types": "^5.20260819.1",
		"bleeps-common": "workspace:*",
		"fs-extra": "^11.4.0",
		"handlebars": "^4.7.9",
		"ldenv": "^0.6.0",
		"prettier": "^3.9.6",
		"typescript": "~6.0.3",
		"viem": "^2.55.19",
		"wait-on": "^9.1.0",
		"wrangler": "^4.124.0"
	}
}
